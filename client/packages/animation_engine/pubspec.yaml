name: animation_engine
description: JSON 指令驱动的动画状态机
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  pet_renderer:
    path: ../pet_renderer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
