name: pet_renderer
description: 可替换的宠物角色渲染器 (Rive 2.5D / Thermion 3D)
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.13.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
